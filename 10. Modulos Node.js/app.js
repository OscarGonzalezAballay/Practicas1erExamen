const sumar = require('./Suma'); //importa la funcion sumar desde suma.js

const resultado = sumar(5, 3); //Llama a la funcion sumar con los numeros escritos.
console.log(`El resultado es: ${resultado}`); //imprime el resultado
